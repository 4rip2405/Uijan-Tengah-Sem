import 'package:flutter/material.dart';

class chord4 extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Tutorial Squat"),
        backgroundColor: Colors.black,
        centerTitle: true,
      ),
    );
  }
}