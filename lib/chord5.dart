import 'package:flutter/material.dart';

class chord5 extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Tutorial Leg Lift"),
        backgroundColor: Colors.black,
        centerTitle: true,
      ),
    );
  }
}